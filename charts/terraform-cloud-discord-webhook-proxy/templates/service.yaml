apiVersion: v1
kind: Service
metadata:
  name: {{ include "terraform-cloud-discord-webhook-proxy.fullname" . }}
  labels:
    {{- include "terraform-cloud-discord-webhook-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "terraform-cloud-discord-webhook-proxy.selectorLabels" . | nindent 4 }}
